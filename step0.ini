Open the main directory in the terminal and type: "npx create-next-app@latest" and then name your project: "client" and then choose "yes" for everything except "src/ directory" and "import alias (@/*)"